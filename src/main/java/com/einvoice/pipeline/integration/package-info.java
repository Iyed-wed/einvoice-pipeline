/**
 * External-system entry points that feed the same invoice engine. Currently the
 * SAP gateway (documented OData/RFC approach, no live connection in this POC).
 */
package com.einvoice.pipeline.integration;
