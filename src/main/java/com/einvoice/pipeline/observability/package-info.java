/**
 * Observability — application metrics expressed against the Micrometer
 * abstraction. Kept separate from the business services so the choice of
 * monitoring backend never leaks into domain logic.
 */
package com.einvoice.pipeline.observability;
